<% content_for :title, "New customer" %>

<%= turbo_frame_tag "modal" do %>
  <%= render Shared::ModalComponent.new(title: "New customer", subtitle: "Add a new customer") do %>
    <%= render "form", customer: @customer %>
  <% end %>
<% end %>
