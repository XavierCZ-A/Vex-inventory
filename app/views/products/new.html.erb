<% content_for :title, "New product" %>

<%= turbo_frame_tag "modal" do %>
  <%= render Shared::ModalComponent.new(title: "New product", subtitle: "Add a new product") do %>
    <%= render "form", product: @product %>
  <% end %>
<% end %>