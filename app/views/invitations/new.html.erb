
<%= turbo_frame_tag "modal" do %>
  <%= render Shared::ModalComponent.new(title: "Invite a new user", subtitle: "Generate a new invitation to join the organization") do %>
    <%= render "form", invitation: @invitation %>
  <% end %>
<% end %>