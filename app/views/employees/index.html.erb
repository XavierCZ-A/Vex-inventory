<% content_for :title, "Employee" %>
<div class="w-full mx-auto p-3">
  <%= render "employees/components/navigation" %>
  <hr class="w-full border-[#E5E7EB] mt-3 mb-5" />
  <% if @users.any? %>
    <div id="orders" class="overflow-hidden p-2 rounded-lg bg-white border border-gray-200 shadow">
      <section class="relative overflow-x-auto">
        <table class="min-w-full">
          <thead>
            <tr class="border-b border-[#E5E7EB]">
              <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Name</th>
              <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Email</th>
              <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Role</th>
              <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Status</th>
              <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Permissions</th>
            </tr>
          </thead>
          <tbody class="bg-white divide-y divide-gray-200">
            <% @users.each do |user| %>
              <%= render "employees/employee", user: user %>
            <% end %>
          </tbody>
        </table>
      </section>
    </div>
  <% else %>
    <%= render Shared::EmptyStateComponent.new(
      title: "There are no employees",
      description: "This provider has no employees added. Do you want to invite one now?",
      icon: "users-group",
      icon_button: "plus",
      path: invitations_path,
      text: "Invite employee"
    ) %>
  <% end %>
</div>
