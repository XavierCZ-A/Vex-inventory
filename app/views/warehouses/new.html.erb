<% content_for :title, "New warehouse" %>

<%= turbo_frame_tag "modal" do %>
  <%= render Shared::ModalComponent.new(title: "New warehouse", subtitle: "Add a new warehouse") do %>
    <%= render "form", warehouse: @warehouse %>
  <% end %>
<% end %>

