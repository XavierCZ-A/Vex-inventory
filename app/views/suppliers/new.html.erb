<% content_for :title, "New supplier" %>

<%= turbo_frame_tag "modal" do %>
  <%= render Shared::ModalComponent.new(title: "New supplier", subtitle: "Add a new supplier") do %>
    <%= render "form", supplier: @supplier %>
  <% end %>
<% end %>
