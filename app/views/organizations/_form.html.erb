
<%= form_with(model: organization, class: "contents") do |form| %>
  <% if organization.errors.any? %>
    <div id="error_explanation" class="bg-red-50 text-red-500 px-3 py-2 font-medium rounded-md mt-3">
      <h2><%= pluralize(organization.errors.count, "error") %> prohibited this organization from being saved:</h2>

      <ul class="list-disc ml-6">
        <% organization.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="my-5">
    <%= form.label :name %>
    <%= form.text_field :name, class: ["block shadow-sm rounded-md border px-3 py-2 mt-2 w-full", {"border-gray-400 focus:outline-blue-600": organization.errors[:name].none?, "border-red-400 focus:outline-red-600": organization.errors[:name].any?}] %>
  </div>

  <h3>Usuario principal</h3>

  <%= form.fields_for :users do |user_fields| %>
    <%= user_fields.label :name %>
    <%= user_fields.text_field :name %>

    <%= user_fields.label :last_name %>
    <%= user_fields.text_field :last_name %>

    <%= user_fields.label :email_address %>
    <%= user_fields.email_field :email_address %>

    <%= user_fields.label :password %>
    <%= user_fields.password_field :password %>
  <% end %>

  <div class="inline">
    <%= form.submit class: "w-full sm:w-auto rounded-md px-3.5 py-2.5 bg-blue-600 hover:bg-blue-500 text-white inline-block font-medium cursor-pointer" %>
  </div>
<% end %>
