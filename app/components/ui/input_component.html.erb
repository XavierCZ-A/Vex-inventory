<div class="absolute inset-y-0 left-0 pl-3 flex items-center pointer-events-none">
  <%= helpers.tabler_icon(icon, size: '15', class: icon_classes) if icon.present? %>
</div>
<%= form.send("#{type}_field", attribute, input_options) %>

